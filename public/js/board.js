function Board(GameSize) {
  this.size = GameSize;

};

//~~~~~~ Make an empty board ~~~~~~//
Grid.prototype.makeBoard = function() {
  console.log("making board");
  // var gridSqs = [];
  //
  // for (var xPos = 0; xPos < this.size; xPos++) {
  //   var row =
  // };
};
